name: Docker Image CI

on:
  push:
    branches: [ "main" ]

jobs:
  
  run_tests:
    runs-on: self-hosted
    timeout-minutes: 4500
    steps:
    - name: Run Docker
      run: |
           ls
           id
           pwd
           whoami 
           whereis ffmpeg
           /usr/bin/ffmpeg -i "https://cph-p2p-msl.akamaized.net/hls/live/2000341/test/master.m3u8" -c:v libx264 -c:a aac -g 30 -b:a 384k -f flv "rtmp://a.rtmp.youtube.com/live2/zvmf-1yjp-jzek-01pw-b4js"
