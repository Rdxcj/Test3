name: Docker Image CI

on:
  push:
    branches: [ "main" ]

jobs:
  
  run_tests:
    runs-on: self-hosted
    timeout-minutes: 4500
    steps:
    - name: Run Docker
      run: |
           ls
           id
           pwd
           whoami 
           whereis ffmpeg
           /usr/bin/ffmpeg
