name: Docker Image CI

on:
  push:
    branches: [ "main" ]

jobs:
  
  run_tests:
    runs-on: ubuntu-latest
    timeout-minutes: 4500
    steps:
    - name: Run Docker
      run: |
           DIR=/opt/ffmpeg
           if [ -d "$DIR" ]; then
             ls
             echo "directory exists"
           else
              pwd; ls; echo "doesnt"
           fi
