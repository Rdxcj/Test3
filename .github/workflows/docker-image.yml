name: Docker Image CI

on:
  push:
    branches: [ "main" ]

jobs:
  
  run_tests:
    runs-on: self-hosted
    timeout-minutes: 4500
    steps:
    - name: Run Docker
      run: |
           ls
           id
           pwd
           whoami 
           whereis ffmpeg
           /usr/bin/ffmpeg -i "https://cph-p2p-msl.akamaized.net/hls/live/2000341/test/master.m3u8"
